<div dir="rtl">

# كيفية تشغيل النظام — RestaurantMS

---

## أولاً: متطلبات التشغيل

قبل البدء، تأكد من توفّر ما يلي على جهازك:

| المتطلب | الإصدار الأدنى | رابط التنزيل |
|---------|---------------|-------------|
| .NET SDK | 8.0 | https://dotnet.microsoft.com/download/dotnet/8.0 |
| SQL Server | 2019 أو أحدث | (يمكن استخدام LocalDB المرفق مع Visual Studio) |
| Git | أي إصدار | https://git-scm.com |

> **ملاحظة:** إذا كنت تستخدم Visual Studio، فإن LocalDB يُثبَّت تلقائياً معه.

---

## ثانياً: خطوات الإعداد بالتفصيل

### الخطوة 1 — استنساخ المشروع

افتح موجه الأوامر (Terminal أو Command Prompt) ثم نفِّذ:

```bash
git clone <رابط-المستودع>
cd RestaurantApp_v2
```

---

### الخطوة 2 — ضبط بيانات الاتصال بقاعدة البيانات

افتح الملف `appsettings.json` في أي محرر نصي، وعدِّل سطر الاتصال:

```json
"ConnectionStrings": {
  "DefaultConnection": "Server=(localdb)\\mssqllocaldb;Database=RestaurantMS;Trusted_Connection=True;MultipleActiveResultSets=true"
}
```

**إذا كنت تستخدم SQL Server Express:** استبدل `(localdb)\\mssqllocaldb` بـ `.\\SQLEXPRESS`

**إذا كنت تستخدم SQL Server عادي:** استخدم `Server=اسم-الخادم` مع بيانات الاعتماد المناسبة.

---

### الخطوة 3 — إنشاء ملف الترحيل (Migration)

```bash
dotnet ef migrations add InitialCreate
```

سيقوم هذا الأمر بإنشاء مجلد `Migrations/` يحتوي على تعريف هيكل قاعدة البيانات.

---

### الخطوة 4 — تطبيق الترحيل على قاعدة البيانات

```bash
dotnet ef database update
```

سيتم إنشاء قاعدة البيانات وجميع الجداول تلقائياً.

---

### الخطوة 5 — تشغيل التطبيق

```bash
dotnet run
```

عند النجاح، ستظهر رسالة مشابهة لـ:

```
Now listening on: https://localhost:5001
Now listening on: http://localhost:5000
```

افتح المتصفح واذهب إلى: `https://localhost:5001`

---

## ثالثاً: أول تسجيل دخول

بعد تشغيل التطبيق لأول مرة، يتم إنشاء حساب المسؤول تلقائياً:

| الحقل | القيمة |
|-------|--------|
| اسم المستخدم | `admin` |
| كلمة المرور | `Admin@123` |
| رقم PIN | `1234` |
| الدور | SuperAdmin (مسؤول النظام) |

> **تنبيه أمني:** غيِّر كلمة المرور ورقم PIN فور تسجيل الدخول الأول!

---

## رابعاً: كيف تفتح POS Touch (شاشة البيع)

1. سجِّل الدخول بحساب الكاشير أو المدير
2. من القائمة الجانبية، اضغط على **"POS"**
3. اختر **"POS Touch"** للوضع اللمسي

**في شاشة POS Touch يمكنك:**
- اختيار المنتجات من القائمة أو البحث باسمها
- تغيير الكميات وإزالة عناصر
- تطبيق خصم (يتطلب PIN المدير إذا تجاوز 10%)
- اختيار طريقة الدفع: **نقدي / بطاقة / مختلط**
- تعليق الطلب (Hold) واستدعائه لاحقاً
- إلغاء الطلب (Void) بموافقة المدير

---

## خامساً: كيف تفتح تسليم الخزينة (Cash Handover)

### فتح جلسة الخزينة (الكاشير)

1. من القائمة الجانبية، اضغط **"Cash Handover"**
2. اضغط **"Open Handover"**
3. أدخل مبلغ بداية الوردية (Opening Float)
4. اضغط تأكيد

> تظل الجلسة مفتوحة طوال الوردية، ويتم تسجيل كل عملية بيع نقدية تلقائياً.

### إغلاق جلسة الخزينة (الكاشير)

1. اضغط **"Close Handover"**
2. أدخل المبلغ الفعلي المتواجد في الخزينة
3. أضف ملاحظاتك إن وجدت
4. اضغط **"Close"**

يقوم النظام بحساب الفرق تلقائياً:
- إذا كان الفرق ضمن الحد المسموح (5 جنيه)، تنتقل الحالة إلى **"معلقة للموافقة"**
- إذا تجاوز الحد، تُصنَّف كـ **"موضوع خلاف" (Flagged)**

### موافقة المدير

1. يفتح المدير **"Handover History"**
2. يختار الجلسة المعلقة
3. يدخل **PIN** المدير ويوافق أو يرفض مع ملاحظة

---

## سادساً: كيف تطبع الفواتير

### طباعة إيصال بعد الدفع

بعد إتمام الدفع في شاشة POS، يظهر زر **"Print Receipt"** تلقائياً.
اضغطه لإرسال الطلب إلى الطابعة الحرارية المضبوطة كطابعة افتراضية.

### طباعة تقرير Z (نهاية اليوم)

1. من القائمة، اضغط **"Reports"**
2. اختر **"Z-Report"** وحدد التاريخ
3. اضغط أيقونة **"Print"** في أعلى الصفحة

### طباعة تقرير X (الوردية الحالية)

1. من **"Reports"** اختر **"X-Report"**
2. اضغط **"Print"** — سيطبع تقرير الوردية الحالية

### إعداد الطابعات

من لوحة التحكم (SuperAdmin):
1. اذهب إلى إعدادات الطابعات
2. أضف طابعة جديدة (IP + Port لطابعات الشبكة)
3. اختر نوع الطابعة: إيصال حراري / فاتورة A4 / طلب مطبخ
4. فعِّل الطابعة الافتراضية لكل نوع

---

## ملاحظات مهمة

- يدعم النظام اللغة **العربية والإنجليجية** — يمكن التبديل من أعلى الصفحة
- جميع البيانات المحذوفة تُحفظ في قاعدة البيانات (Soft Delete) ولا تُحذف فعلياً
- رقم PIN للمدير يُشفَّر بـ BCrypt ولا يُخزَّن كنص عادي
- سجل كامل للعمليات متاح في ملف `logs/` في مجلد التطبيق

</div>
